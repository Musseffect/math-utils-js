

describe.skip('Least squares', () => {
    test('Linear', () => {
        // aN + a0x0 + a1x1 + a2x2 + a3x3 = y
    });
    test('Lin log', () => {
        // a + b * l(x)
    });
    test('Bilinear', () => {
        // z = a00 + a10x + a01y + a11xy
    });
    test('Gaussian', () => {

    });
});
